# Generated Templates Directory

This directory contains templates generated by the LLM-driven template generation system.

Each generated template includes:
- `.js` - Transpiled p5.js template code
- `.dsl.json` - Original DSL JSON specification
- `.meta.json` - Evaluation metadata and claims
- `.png` - Reference screenshot

Generated templates can be manually reviewed and integrated into the main system by:
1. Copying the `.js` file to `templates/` directory
2. Registering the template in `services/artGenerator.js`
3. Adding a corresponding schema to `schemas/` directory
